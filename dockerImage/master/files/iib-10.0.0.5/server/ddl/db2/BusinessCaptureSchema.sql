CONNECT TO MBRECORD;

CREATE TABLE WMB_BUSTRANS
(
 ID VARCHAR(100) NOT NULL,
 FIRST_SEEN_TIMESTAMP VARCHAR(100), 
 LAST_SEEN_TIMESTAMP VARCHAR(100),   
 BTD_NAME VARCHAR(100) NOT NULL, 
 STATUS CHAR(1), 
 WMB_MSGKEY VARCHAR(100) NOT NULL,
 SEQ_NO INTEGER   NOT NULL, 
 SEQ_TIME BIGINT,
 ROLE INTEGER,
 UNIQUE_FLOW_NAME VARCHAR(500)
)
 DATA CAPTURE NONE;

ALTER TABLE WMB_BUSTRANS ADD CONSTRAINT WMB_BUSTRANS_PK PRIMARY KEY (ID,BTD_NAME,SEQ_NO);

TERMINATE;
