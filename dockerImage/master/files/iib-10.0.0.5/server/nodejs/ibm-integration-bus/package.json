{
  "name": "ibm-integration-bus",
  "version": "1.0.0-20160123-1400",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "echo do nothing",
    "cover": "echo do nothing",
    "check-coverage": "echo do nothing",
    "pretest": "jshint .",
    "test": "istanbul cover $npm_package_config_cov_dir $npm_package_config_root_dir _mocha -- -R spec test",
    "posttest": "istanbul check-coverage --statements 100 $npm_package_config_cov_file",
    "watch:test": "nodemon --exec 'npm test' -i node_modules -i coverage"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.4.1",
    "istanbul": "^0.4.2",
    "jshint": "^2.9.1",
    "mocha": "^2.3.4",
    "mockery": "^1.4.0",
    "nodemon": "^1.8.1",
    "sinon": "^1.17.2",
    "sinon-chai": "^2.8.0"
  },
  "dependencies": {
    "log4js": "^0.6.30"
  }
}
