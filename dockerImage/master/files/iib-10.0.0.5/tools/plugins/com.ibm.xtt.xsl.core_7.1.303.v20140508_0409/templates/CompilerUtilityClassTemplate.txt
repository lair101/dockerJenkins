${package_declaration}
import java.util.Properties;

import javax.xml.transform.Transformer;
import javax.xml.transform.TransformerFactory;
import javax.xml.transform.stream.StreamResult;
import javax.xml.transform.stream.StreamSource;

public class ${utility_class_name} {

	/**
	 * ${main_method_description}
	 */
	public static void main(String[] args) {
		if(args.length == 2) {
			StreamSource streamSource = new StreamSource(args[0]); // ${path_source_document}
			StreamResult streamResult = new StreamResult(args[1]); // ${path_result_document}
			runTransformation(streamSource, streamResult);
		}
	}
	
	/**
	 * ${transformation_method_description}
	 * 
	 * @param streamSource ${transformation_input}
	 * @param streamResult ${transformation_output}
	 */
	public static void runTransformation(StreamSource streamSource, StreamResult streamResult) {
		Properties properties = System.getProperties();
		properties.put("javax.xml.transform.TransformerFactory", "org.apache.xalan.xsltc.trax.TransformerFactoryImpl"); //$NON-NLS-1$ //$NON-NLS-2$
		System.setProperties(properties);
		TransformerFactory transformerFactory = TransformerFactory.newInstance();
		transformerFactory.setAttribute("use-classpath" , "true"); //$NON-NLS-1$ //$NON-NLS-2$
		transformerFactory.setAttribute("package-name", "${translet_package_name}"); //$NON-NLS-1$ //$NON-NLS-2$
		try {
			Transformer transformer = transformerFactory.newTransformer(new StreamSource("${translet_class_name}")); //$NON-NLS-1$
			transformer.transform(streamSource, streamResult);
		} catch(Exception exception) {exception.printStackTrace();}
	}

}
